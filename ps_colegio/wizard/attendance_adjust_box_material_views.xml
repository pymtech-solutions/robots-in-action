<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard form view -->
    <record id="view_reponer_wizard_form_attendance" model="ir.ui.view">
        <field name="name">oe.refill.wizard.form.attendance</field>
        <field name="model">attendance.adjust.box.material</field>
        <field name="arch" type="xml">
            <form string="Ajuste de Material">
                <group>
                    <field name="attendance_id" invisible="1"/>
                    <field name="box_id" invisible="0" domain="[('id', 'in', attendance_box_ids)]"/>
                    <field name="box_product_ids" widget="many2many_tags" invisible="1"/>
                    <field name="product_id" domain="[('id', 'in', box_product_ids)]"
                           options="{'no_create': True, 'no_edit': True}"/>
                    <field name="movement_type"/>
                    <field name="qty"/>
                    <field name="notes"/>
                </group>
                <footer>
                    <button string="Confirmar" type="object" name="action_confirm" class="btn-primary"/>
                    <button string="Cancelar" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Wizard action -->
    <record id="action_reponer_wizard_attendance" model="ir.actions.act_window">
        <field name="name">Reponer Producto</field>
        <field name="res_model">attendance.adjust.box.material</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
