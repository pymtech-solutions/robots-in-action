<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard form view -->
    <record id="view_reponer_wizard_form" model="ir.ui.view">
        <field name="name">oe.refill.wizard.form</field>
        <field name="model">adjust.box.material</field>
        <field name="arch" type="xml">
            <form string="Ajuste de Material">
                <group>
                    <field name="box_id" invisible="0"/>
                    <field name="box_product_ids" widget="many2many_tags" invisible="1"/>
                    <field name="product_id" domain="[('id', 'in', box_product_ids)]"
                           options="{'no_create': True, 'no_edit': True}"/>
                    <field name="movement_type"/>
                    <field name="qty"/>
                    <field name="notes"/>
                </group>
                <footer>
                    <button string="Confirmar" type="object" name="action_confirm" class="btn-primary"/>
                    <button string="Cancelar" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Wizard action -->
    <record id="action_reponer_wizard" model="ir.actions.act_window">
        <field name="name">Reponer Producto</field>
        <field name="res_model">adjust.box.material</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
