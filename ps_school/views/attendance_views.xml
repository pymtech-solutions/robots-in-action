<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="course_line_attendance_list_view" model="ir.ui.view">
            <field name="name">course.line.attendance.tree.view</field>
            <field name="model">school.attendance</field>
            <field name="arch" type="xml">
                <list>
                    <field name="date"/>
                    <field name="school_id" readonly="1"/>
                    <field name="course_line_id"/>
                    <field name="course_schedule_ids" widget="many2many_tags" readonly="1"/>
                    <field name="teacher_ids" widget="many2many_tags" readonly="1"/>
                    <field name="substitute_teacher_ids" widget="many2many_tags"/>
                    <field name="materials_status"/>
                </list>
            </field>
        </record>

        <!--Form view-->
        <record id="course_line_attendance_form_view" model="ir.ui.view">
            <field name="name">course.line.attendance.form.view</field>
            <field name="model">school.attendance</field>
            <field name="arch" type="xml">
                <form string="Attendance">
                    <header>
                        <button name="action_adjust_materials" string="Modificar material"
                                type="object" class="btn-primary" invisible="materials_status == 'closed'"/>
                        <button name="action_close_attendance" string="Cerrar asistencia"
                                type="object" class="btn-success" invisible="materials_status == 'closed'"/>
                        <button name="action_reopen_attendance" string="Reabrir asistencia"
                                type="object" class="btn-warning" invisible="materials_status != 'closed'"/>
                        <field name="materials_status" widget="statusbar"
                               statusbar_visible="review,closed"/>
                    </header>
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button class="oe_stat_button" type="object" name="action_view_material_movements"
                                    icon="fa-exchange">
                                <field string="Movimientos" name="material_movement_ids" widget="statinfo"/>
                            </button>
                        </div>
                        <group>
                            <group>
                                <field name="course_line_id"/>
                                <field name="school_id" readonly="1"/>
                                <field name="program_id" readonly="1"/>
                                <field name="program_subject_ids" invisible="1"/>
                                <field name="subject_id" domain="[('id', 'in', program_subject_ids)]"/>
                                <field name="date"/>
                            </group>
                            <group>
                                <field name="course_schedule_ids" widget="many2many_tags" readonly="1"/>
                                <field name="teacher_ids" widget="many2many_tags" readonly="1"/>
                                <field name="substitute_teacher_ids" widget="many2many_tags" domain="[('id', 'not in', teacher_ids)]"/>
                                <field name="box_ids" readonly="1" widget="many2many_tags"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Registros de Asistencia">
                                <field name="attendance_line_ids">
                                    <list editable="bottom">
                                        <field name="student_id" domain="[('id', 'in', student_domain_ids)]"/>
                                        <field name="attended"/>
                                    </list>
                                </field>
                            </page>
                            <page string="Historial de Movimientos">
                                <button name="action_undo_material_movements" string="Deshacer movimientos de materiales"
                                        type="object" class="btn-warning" invisible="materials_status == 'closed'"/>
                                <field name="material_movement_ids">
                                    <list create="0" edit="0">
                                        <field name="date"/>
                                        <field name="box_id"/>
                                        <field name="product_id"/>
                                        <field name="movement_type"/>
                                        <field name="qty"/>
                                        <field name="notes"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Attendance action-->
        <record id="action_school_attendance" model="ir.actions.act_window">
            <field name="name">Assistance</field>
            <field name="res_model">school.attendance</field>
            <field name="view_mode">list,form</field>
            <field name="context">
                {
                    'group_by': 'date',
                    'orderedBy': [{'name': 'date', 'asc': False}]
                }
            </field>
            <field name="help" type="html">
                <p>
                    No hay ninguna asistencia que mostrar.
                </p>
            </field>
        </record>

        <menuitem name="Asistencia"
                  id="menu_school_attendance"
                  parent="menu_school"
                  sequence="40"
                  groups="ps_school.group_school_teacher,ps_school.group_school_manager"/>


        <menuitem name="Asistencia"
                  id="menu_school_attendance_action"
                  parent="menu_school_attendance"
                  action="action_school_attendance"
                  sequence="40"
                  groups="ps_school.group_school_teacher,ps_school.group_school_manager"/>
    </data>
</odoo>